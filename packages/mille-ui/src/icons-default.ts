// Public entry for `@vibecook/mille-ui/icons/default`.
//
// Tiny re-export of the default theme so consumers that only want the
// fallback icon set can import it in isolation without pulling in the
// resolver / loader.

export { defaultIconTheme } from './icons/default-theme.js';
