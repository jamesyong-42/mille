// Phase 5.2 — explorer views / scopes subpath entry.
//
// Exposed as `@vibecook/mille-ui/views`. Pure projectors + resolve + a
// virtualized flat list. Does not fork FileTree.

export * from './views/index.js';
