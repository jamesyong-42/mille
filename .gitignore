# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# Node
node_modules/
dist/
.turbo/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# napi build artifacts
# Ignore .node binaries everywhere *except* inside the published per-platform
# packages, which legitimately contain real .node files.
*.node
!packages/file-explorer-*/**/*.node

# napi-rs artifacts dir (output of `napi build` before distribution)
artifacts/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE / editors
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*
!.env.example
